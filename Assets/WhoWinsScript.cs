using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class WhoWinsScript : MonoBehaviour {

	// Use this for initialization
	//ShaderVariantCollection c1,c2;
	bool win;

	void Start () {/*
		GameObject player1 = GameObject.Find ("c1_1");
		GameObject player2 = GameObject.Find ("c2_1");

		var player1script = player1.GetComponents<characterController> ();
		var player2script = player2.GetComponents<CharacterController2> ();


		win = (player1script.dead) ? true : false;

		Text me;
		me = GetComponents<Text> ();
		if (win) {
			me.text = "2P WINS !!";
		} else {
			me.text = "1P WINS !!";
		}*/


	}
	
	// Update is called once per frame
	void Update () {
	}
}
